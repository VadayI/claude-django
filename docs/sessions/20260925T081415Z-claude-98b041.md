---
session: 20260925T081415Z-claude-98b041
agent: claude
recorded: 2026-09-25T08:14:15Z
branch: feat/p07-shared-memory
revision: 2f74f7fb4ba7107ff9d2df5a7cd5f39b0dea6d7b
---

# Session 20260925T081415Z-claude-98b041

<!-- Fill every section before committing. Record facts with their source; never
paste transcripts, env values, tokens or raw logs. -->

## Task

Close the P07 leftovers in Django: the shared language preference, and migration of the template's own Claude-only language file on real data.

## Changes

- Commit `2f74f7f`: development pin `f8e3162`. The template's own
  `.claude/rules/output-language.md` (Українська) was migrated with the tool into
  `docs/ai/overrides/output-language.md`, leaving a pointer; a repeated apply
  changed nothing. AGENTS, the output-language rule, bootstrap step 5,
  `/doctor` step 0, `/set-language`, template-sync, `/update-from-template` and
  production-structure are updated; `seed_preflight` rejects `docs/ai/overrides/`.
- This record and the `docs/HANDOFF.md` entry.

## Decisions

- Shared preference `docs/ai/overrides/output-language.md`; the legacy
  `.claude/rules/output-language.md` becomes a fixed pointer, so exactly one
  writable preference remains, and old CLAUDE.md imports keep working.
- A conflict is never resolved by the tool alone: the user's current choice
  goes into the shared file, then `--language --apply --keep-shared` runs.
- The start context shows the effective language to every agent. An unmigrated
  legacy file is not a finding; a conflict is.

## Checks

- `python3.13 scripts/ai/core_sync.py --check` — PASS (no drift)
- `python3.13 scripts/ai/generate_adapters.py --check` — PASS
- `python3.13 scripts/build_instruction_manifest.py --check` — PASS
- `python3.13 -m unittest discover -s tests -p "test_*.py"` — 38 OK
- `python3.13 scripts/ai/project_state.py --root . --language` — status canonical, Українська
- Cloud Linux container, Python 3.13.13, Node 22.22.2, Git 2.43.0. Candidate `2f74f7f`, base `1fa8fbc`.

## Limitations

- Windows-native runs of `project_state --language` and `session_context.py`: NOT_VERIFIED
  (Linux fixtures cover CRLF/BOM and a cp1252 stream).
- The runtime acceptance run (Claude → Codex → Claude, parallel sessions) is written
  down but NOT_VERIFIED until someone executes it.
- Hosted CI for these commits: pending the push. The core pin is a development pin.

## Next step

After contract #66 is merged on the user's command: repin to the integrated core and restore «Tymczasowy pin deweloperski»; then #45 → #46. The runtime acceptance run follows docs/ai/session-continuity.md.
