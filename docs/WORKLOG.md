# WORKLOG — claude-django

> Записи H1 2026 (2026-05-30 … 2026-06-09) перенесено в `docs/WORKLOG-2026-H1.md`
> (аудит v2, батч L). Цей файл продовжує append-only хроніку.

## 2026-07-07 — main — Глибокий аудит v2 (після A–E) + батчі F–L: контракт, Windows, плагіни, дієта, /adopt

- Аудит: 4 паралельні read-only агенти (команди/агенти; плагіни по фактичних клонах superpowers v6.1.1 / engineering v1.2.0 / claude-plugins-official 255 шт.; rules/templates/scripts; контракт-шви по клону claude-api-contract main + 5 тегів). Батчі A–E верифіковані як чисті. Звіт: `docs/reviews/2026-07-07-deep-audit-v2.md`.
- Рішення мейнтейнера (5): engineering → персональні; superpowers → офіційний marketplace; trailing slash → `trailing_slash=False` у шаблоні; adopt → окрема команда `/adopt`; демоція — повний перелік 9 правил.
- **F (контракт-дефолти):** пін v0.2.0 у `.env.example` ×2 (v0.4.0 — мертвий тег без openapi.yml); нормалізація `CONTRACT_REPO` (full URL → slug) у `pull_contract.sh`; приклад `CONTRACT_URL` без реального IP і «Prism mock»; `templates/api_INDEX.md` RFC 7807 → envelope ADR 0020; sync `templates/.env.example` з кореневим (ADR 0008 permissions, ADR 0023 wrapper).
- **G (Windows + залишки, 13 файлів):** gh на нативному Windows за ADR 0022 (environment.md Scope 1, doctor.md платформо-залежний вердикт + winget, Prereq-блоки preflight/create-pr/review-pr/fix-ci, bootstrap `NO_GH_BIN` + winget); mini-frontend/PR-per-layer прибрано (reviewer, review-pr); ручний лог ×3 прибрано (set-language, synthesize-brief, update-from-template + перенумерація кроків); стале «ADR 0005 PowerShell» у create-pr → ADR 0022; guides.md WORKLOG→ADR; приписка в ADR 0014.
- **I (плагіни, ADR 0024):** `enabledPlugins` 5→4 — engineering у персональні (6/10 перетинів + другий github-конектор), `superpowers@claude-plugins-official`; environment.md Scope 2 переписано; paste-блоки bootstrap/plugins синхронізовано, config-check/plugins без хардкод-списків (канон — environment.md); precedence note workflow.md розширено на test-driven-development/systematic-debugging; mcp-stack `query-docs`→`get-library-docs`; MCP allowlist-и 5 агентів (`mcp__playwright`→qa, `mcp__github`→reviewer/docs-writer, `mcp__context7`→api-architect/django-developer); анти-кандидати (feature-dev, pr-review-toolkit, commit-commands) і кандидати (pyright-lsp, security-guidance) зафіксовані в ADR 0024; приписка в ADR 0011; todo: CLI-верифікація MCP-доступу.
- **H (контракт-дисципліна, ADR 0025):** `pull_contract.sh --check` завжди по піну — `CONTRACT_URL` fetch-only (+hint у помилці); backend-ci.yml коментарі (CONFORMANCE_BASE_URL для MVP/production; drift-гейт «озброюється» через vars); bootstrap ставить `gh variable set CONTRACT_VERSION`; environment.md Scope 3 + рядок «CI drift-gate armed»; no-trailing-slash канон (architecture.md) + виправлені api-docs/api-architect/verification/api_INDEX/drf-api-design; verification.md — мапінг двох незалежних `endpoints.json` (контрактного і бекендного).
- **J (контекст-дієта):** import-блок CLAUDE.md 16→7 правил (демотовано 9: api-docs, app-readme, code-style, simplicity-surgical, environment, project-maturity, verification, user-guides, deviation-register — усі на tier-2 @-цитати); Agent Dispatch стиснуто до вказівника на workflow.md; de-@ демотованих у глобальних файлах (CLAUDE.md, workflow, preflight); нові @-цитати: reviewer (verification, deviation-register), ba (project-maturity), django-developer (api-docs, simplicity-surgical), api-architect/docs-writer (api-docs, app-readme); хедери «auto-loaded»→«per-agent via @-reference» ×4; living-plan — plain-вказівник на deviation-register; Rule scoping у CLAUDE.md переписано; todo: CLI-верифікація демоції. Anti-drift преамбулу ×3 свідомо НЕ дедуплено (взаємні посилання вже існують, текст контекстний — P3, Simplicity First).
- **K (adopt, ADR 0026):** нова команда `/adopt` (клон → layout survey → гілка → template-sync MODE=adopt → звіт → PR); Adopt mode у template-sync (адитивність, `*.adopt-proposed`, `templates/` не wholesale, lineage-маркер `mode: adopt`); `/doctor` — 5-й сценарій `foreign-django`, `existing-incomplete` уточнено як template-derived; bootstrap guard-и (Mode B без lineage → STOP → /adopt; BACKEND_WITHOUT_GIT → /adopt); install.sh — foreign-guard (die на manage.py/CLAUDE.md/Makefile/docker-compose.yml/.gitignore без `--force`, перевірено запуском) + `seed()` з `.bak`; README — Quick start чесно перейменовано (greenfield) + розділ «Adopting into an existing (foreign) Django project»; реєстрації в CLAUDE.md/workflow.md/inventory.md.
- **L (гігієна):** WORKLOG H1 (43 записи) → `docs/WORKLOG-2026-H1.md`; `docs/HANDOFF.md` регенеровано (був stale з 2026-06-09); кореневий untracked HANDOFF.md → `LOCAL/HANDOFF-template-dev.md`; bootstrap футер-дата.
- Механіка: усі правки bash+python через /dev/shm→cp (Edit/Write на 9p-mount заборонені), кожен файл cmp + NUL-чек; settings.json перевірено json.load; backend-ci.yml — PyYAML; install.sh — bash -n + функціональний тест guard-ів; pull_contract.sh — bash -n + тест нормалізації. Коміти — з host shell (3: звіт / конфіг F–K / docs L).
- Лишається: батч M — окремий PR у claude-api-contract (README Status: v0.3.0+ = template releases; consumer-секція — реальний механізм піна замість check_contract_sync.sh/lock.json; узгодити форму CONTRACT_REPO); CLI-верифікації з `docs/todo.md` (log-hook, MCP allowlist, демоція); справжній контрактний реліз (v0.5.0) у contract-репо, після чого підняти пін тут PR-ом + `gh variable set`.
